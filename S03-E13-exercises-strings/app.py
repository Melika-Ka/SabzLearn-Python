# یک رشته را از کاربر بگیر ید و سپس تعداد جمله ها، تعداد کلمه ها ، تعداد کل کارکتر ها، و تعداد حروف انگلسی را چاپ کنید .
# user_txt = input("Enter your text")
# user_txt = "Often, programmers fall in love with Python because of the increased productivity it provides. Since there is no compilation step, the edit-test-debug cycle is incredibly fast. Debugging Python programs is easy: a bug or bad input will never cause a segmentation fault. Instead, when the interpreter discovers an error, it raises an exception. When the program doesn't catch the exception, the interpreter prints a stack trace. A source level debugger allows inspection of local and global variables, evaluation of arbitrary expressions, setting breakpoints, stepping through the code a line at a time, and so on."
# sentences_count = user_txt.count(".") + user_txt.count("?") + user_txt.count("!")
# print("sentences_count :", sentences_count)
# print("length of sentence char :", len(user_txt))
#  یک کاراکتر از کاربر بگیر ید و یون یکد آن را چاپ کن ید.
# user_char = input("Enter your char")
# print(ord(user_char))
# یک شماره همراه را از کاربر بگیر ید و بررسی کن ی د که آیا تمام ارقام آن عدد است یا خیر
# user_num = input("Enter your numbers")
# print(user_num.isnumeric())
# print(user_num.isdigit())
# print(user_num.isdecimal())
#
print("SGDGF\rbccvnbn")